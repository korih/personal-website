{
  "dependencies": {
    "gh-pages": "^6.1.1",
    "three": "^0.164.1"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  }
}
